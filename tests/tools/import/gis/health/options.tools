python -c "import sys,os; sys.path.insert(0, os.path.join('tools','import')); from gis.api import create_app; from fastapi.testclient import TestClient; assert TestClient(create_app()).get('/v1/health').json()['status']=='ok'"
